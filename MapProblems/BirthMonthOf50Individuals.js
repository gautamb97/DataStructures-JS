let birthDays = new Map;
let birthMonth = new Map;
let birthYear = new Map;

let count = 0;

for(let i = 0; i <= 50; i++){
    let individual = 1 + (Math.floor(Math.random() * 10) % 50);
    birthDays.set(individual,);
}